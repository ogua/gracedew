# Dependencies (Node is build-time only for Tailwind; not needed on the host)
node_modules/

# Local secrets / environment overrides
db/config.local.php
production_details.md

# OS / editor cruft
.DS_Store
Thumbs.db

# Claude Code local session state
.claude/settings.local.json
.claude/scheduled_tasks.lock
